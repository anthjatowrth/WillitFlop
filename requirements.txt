# =============================================================================
# DEV ENVIRONMENT SNAPSHOT — not used by any deployment target.
#
# Deployment requirements:
#   API (Render)    → requirements-api.txt   (11 packages)
#   ETL (CI/GitHub) → requirements-etl.txt   (8 packages)
#
# This file was originally a raw `pip freeze` dump (179 packages).
# Audit 2026-03-26: removed 67 packages confirmed to have zero direct imports
# anywhere in api/, ml/, pipeline/, or dbt/. Grouped by reason below.
#
# REMOVED — Scrapy web-scraping framework + its full transitive tree
#   (no `import scrapy` or related imports found anywhere in the codebase):
#   Scrapy, cssselect, itemadapter, itemloaders, parsel, Protego,
#   PyDispatcher, queuelib, w3lib, tldextract, requests-file,
#   Twisted, Automat, constantly, hyperlink, Incremental,
#   zope.interface, service-identity
#
# REMOVED — Matplotlib / Seaborn / Plotly visualisation stack
#   (no `import matplotlib`, `import seaborn`, or `import plotly` found;
#   charts are done entirely in the React frontend with Recharts):
#   matplotlib, seaborn, plotly, contourpy, cycler, fonttools,
#   kiwisolver, narwhals
#
# REMOVED — SHAP explainability + Numba JIT compiler
#   (no `import shap` or `import numba` found; model uses XGBoost
#   with sklearn's built-in feature_importances_ instead):
#   shap, slicer, numba, llvmlite
#
# REMOVED — OpenTelemetry distributed tracing (all 8 packages + gRPC deps)
#   (no opentelemetry import found; Prefect pulls its own telemetry):
#   opentelemetry-api, opentelemetry-exporter-otlp,
#   opentelemetry-exporter-otlp-proto-common,
#   opentelemetry-exporter-otlp-proto-grpc,
#   opentelemetry-exporter-otlp-proto-http, opentelemetry-proto,
#   opentelemetry-sdk, opentelemetry-semantic-conventions,
#   googleapis-common-protos, grpcio, protobuf
#
# REMOVED — SQLAlchemy ORM + Alembic migrations
#   (project uses raw psycopg2 everywhere; no `import sqlalchemy` found):
#   SQLAlchemy, SQLAlchemy-JSONField, SQLAlchemy-Utils,
#   alembic, Mako, greenlet, sqlparse
#
# REMOVED — Flask web framework + unique deps
#   (project uses FastAPI; no `import flask` found):
#   Flask, Werkzeug, itsdangerous
#
# REMOVED — python-slugify text utility
#   (no `import slugify` found):
#   python-slugify, text-unidecode
#
# REMOVED — Sentry error monitoring SDK
#   (no `import sentry_sdk` found; not wired up in any config):
#   sentry-sdk
#
# REMOVED — Async email + async SQLite drivers
#   (no `import aiosmtplib` or `import aiosqlite` found):
#   aiosmtplib, aiosqlite
#
# REMOVED — Playwright browser automation
#   (no `import playwright` found):
#   playwright, pyee
#
# REMOVED — Cadwyn API versioning framework + unique deps
#   (no `import cadwyn` found):
#   cadwyn, wirerope, methodtools, libcst, svcs
#
# FLAGGED (kept, but their direct parent was removed — review on next freeze):
#   pillow         — was matplotlib's image dep; no other confirmed user
#   lockfile       — was python-daemon dep; python-daemon usage unconfirmed
#   python-daemon  — no confirmed import in project code
#   jmespath       — no confirmed import; possibly botocore transitive
#   pyasn1         — parent service-identity removed; still needed by pyOpenSSL
#   pyasn1_modules — same as above
#
# NOTE: prefect version floor corrected from >=2.0 to >=3.0 to match
#       requirements-etl.txt and the Prefect 3 APIs used in pipeline/flow.py
# =============================================================================

a2wsgi==1.10.10
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
argcomplete==3.6.3
asgiref==3.11.1
attrs==25.4.0
beautifulsoup4==4.14.3
blinker==1.9.0
cachetools==5.5.2
certifi==2026.2.25
cffi==2.0.0
charset-normalizer==3.4.5
click==8.3.1
cloudpickle==3.1.2
colorama==0.4.6
colorlog==6.10.1
cron_descriptor==2.0.6
croniter==6.0.0
cryptography==46.0.5
defusedxml==0.7.1
Deprecated==1.3.1
dill==0.4.1
dnspython==2.8.0
email-validator==2.3.0
fastapi==0.135.1
fastapi-cli==0.0.24
fastapi-cloud-cli==0.14.1
fastar==0.8.0
filelock==3.25.1
fsspec==2026.2.0
gunicorn==25.1.0
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.28.1
idna==3.11
importlib_metadata==8.7.1
Jinja2==3.1.6
jmespath==1.1.0
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
lazy-object-proxy==1.12.0
linkify-it-py==2.1.0
lockfile==0.12.2
lxml==6.0.2
markdown-it-py==4.0.0
MarkupSafe==3.0.3
mdurl==0.1.2
more-itertools==10.8.0
msgspec==0.20.0
numpy==2.4.3
packaging==26.0
pandas==3.0.1
pathlib_abc==0.5.2
pathspec==1.0.4
pendulum==3.2.0
pillow==12.1.1
pluggy==1.6.0
prefect>=3.0
psutil==7.2.2
psycopg2-binary==2.9.11
pyasn1==0.6.2
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.12.5
pydantic-extra-types==2.11.0
pydantic-settings==2.13.1
pydantic_core==2.41.5
PyJWT==2.11.0
Pygments==2.19.2
pyOpenSSL==25.3.0
pyparsing==3.3.2
python-daemon==3.1.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.22
pytz==2026.1.post1
PyYAML==6.0.3
referencing==0.37.0
requests==2.32.5
retryhttp==1.4.0
rich==14.3.3
rich-argparse==1.7.2
rich-toolkit==0.19.7
rignore==0.7.6
rpds-py==0.30.0
scikit-learn==1.8.0
scipy==1.17.1
setproctitle==1.3.7
six==1.17.0
soupsieve==2.8.3
starlette==0.52.1
structlog==25.5.0
tabulate==0.10.0
tenacity==9.1.4
termcolor==3.3.0
threadpoolctl==3.6.0
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2025.3
uc-micro-py==2.0.0
universal_pathlib==0.3.10
urllib3==2.6.3
uuid6==2025.0.1
uvicorn==0.41.0
vaderSentiment==3.3.2
watchfiles==1.1.1
websockets==16.0
wrapt==2.1.2
xgboost==3.2.0
zipp==3.23.0
